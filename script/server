#!/bin/sh
# script/server — Start the application (continuous collect loop).
set -eu
cd "$(dirname "$0")/.."

if [ ! -d .venv ]; then
  echo "ERROR: .venv missing. Run script/setup first." >&2
  exit 1
fi

exec .venv/bin/python main.py collect --loop
